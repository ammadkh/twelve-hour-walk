import { colors } from "./colors";
import { space, lineHeights, defaultSpaces } from "./spacing";
import { sizes, inputSize, iconSizes } from "./sizes";
import { fonts, fontWeights, fontSizes } from "./fonts";

export const theme = {
  colors,
  space,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
  inputSize,
  iconSizes,
  defaultSpaces,
};
